dist: trusty
sudo: require
language: c
compiler: gcc

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake

script:
	- cmake -Dtest=ON -DDEBUG=ON .
	- make
	- bin/runUnitTests
	
